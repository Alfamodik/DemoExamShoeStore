<Window x:Class="ShoeStore.Orders"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ShoeStore"
        mc:Ignorable="d"
        Title="Заказы"
        MinHeight="450"
        MinWidth="800"
        Height="450"
        Width="800"
        Icon="/Resources/Icon.png"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Image 
            Grid.Row="0"
            Grid.Column="0"
            Height="100"
            Source="/Resources/Icon.png"
            Margin="10"/>

        <TextBlock
            Grid.Row="0"
            Grid.Column="1"
            Text="Заказы"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            FontSize="40"/>

        <Button
            Grid.Row="0"
            Grid.Column="2"
            Name="CreateButton"
            Content="Создать"
            Height="35"
            Width="100"
            Click="CreateOrder"
            Margin="10" 
            Background="MediumSpringGreen"/>

        <Button
            Grid.Row="0"
            Grid.Column="3"
            Content="Назад"
            Height="35"
            Width="100"
            Click="OpenCatalog"
            Margin="10" 
            Background="MediumSpringGreen"/>

        <ListBox
            Name="OrdersListBox"
            Grid.Column="0"
            Grid.Row="2"
            Grid.ColumnSpan="4"
            HorizontalContentAlignment="Stretch"
            MouseDoubleClick="OrdersListBox_MouseDoubleClick">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <local:OrderUserControl DataContext="{Binding}"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
